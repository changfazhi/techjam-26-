# AGENTS.md

See [`CLAUDE.md`](CLAUDE.md). It is the single source of truth for how to work in this
repository — what the project is, what to read first, current status, the hard rules, the
landmines, the conventions, and the commands.

This file exists only so agents that look for `AGENTS.md` by name find their way there.
Do not copy content into it; keep the guidance in one place so it cannot drift.
